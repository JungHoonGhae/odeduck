# Project skill evaluation

## 2026-09-08 후속 갱신과 독립 행동 확인

최신 사용자 요청은 제품 구현 재개가 아니라 스킬 자체의 갱신이다. `skill-creator`와
`writing-for-agents`에 따라 아래 두 문구만 교체하고, 앞서 정적으로만 확인한 분기를 독립 실행했다.

- 프로젝트 graph 스킬의 사업체 식별자 예시와 필수 평가 사례를 관측 단위·시간 변화·전체/부분 등
  도메인별 연결 위험으로 일반화했다. 고정된 프로젝트 평가 계약은 그대로이며, 새 질문에 무관한
  사업체·브랜드 사례를 강제하지 않는다. 설계에 쓰지 않은 분야의 전이 검증은 유지한다.
- 공용 `ask-matt`에서 진단을 곧바로 수정으로 연결하던 문장을 교체했다. 진단은 원인과 근거,
  수정 요청은 회귀 테스트와 구현으로 이어진다. architecture·wayfinder의 기존 목표·범위 지침은
  재작성하지 않고 아래 실행에서 함께 확인했다. 공용 변경은 다른 프로젝트에도 적용된다.

### 관찰된 행동

부모 대화와 예상 답을 전달하지 않은 독립 에이전트 두 개에 수정 후 스킬과 최소 가상 자료를 제공했다.
네트워크·제품 수정·이슈 쓰기는 금지하고, 임시 자료를 읽는 실제 설계 검토를 요청했다.

| 가상 요청·입력 | 관찰한 결과 |
| --- | --- |
| 관측소별 강수량을 유역별로 비교. 같은 이름의 S1/S2에 900/1200 mm, 2025년 B1/B2 대응, S1의 2026년 B2 대응. 기존 결합기는 있고 검사 결과의 대응표를 계획기가 소비하지 않음 | graph 스킬로 누락 역할을 채우는 후속 탐색을 권고하고 JSON·SQLite 유지. 2025년 기대값 900/1200을 계산하며 2026년 대응·면적 가중 주장과 구분. 사업체 사례를 요구하지 않음 |
| 과거 Notes에는 구현 위임, 최신 요청은 CSV/JSON 구조 검토. CSV는 `>= min`, JSON은 `> min`이며 기준 100, 금액 99·100·101 | ask-matt·wayfinder·architecture를 읽고 함수 호출로 CSV 100·101 대 JSON 101의 차이를 재현. 기존 선택 함수 공유를 권고하고 플러그인·새 지도·구현 변경 없이 검토를 반환 |

둘 다 해당 요청에서 기대한 분기를 보였으나 수정 전후 비교나 반복 성공률 측정은 아니다. graph 사례는
설계 추론이고, export 사례만 가상 코드의 실제 함수 호출이다. 오데덕의 무힌트 검색·실원천 연결·목표
완주 실험과는 별개다. 진단 전용 분기는 문구 정합성만 확인했으며 별도 행동 실험은 하지 않았다.

YAML 파싱·필수 메타데이터·기존 호출 정책·로컬 참조 및 `git diff --check`를 확인했다.
번들 `quick_validate.py`는 PyYAML 부재로 실행하지 못해 Ruby YAML 기반 검사로 대체했다.
검증기를 통과시키려고 의존성을 설치하거나 호출 정책을 변경하지 않았다.
입력 자료와 변경 전 백업은 `/tmp/odeduck-skill-forward.AiANSq/`에 있으며 영구 보존을 보장하지 않는다.
검사한 SKILL.md SHA-256은 graph `a010c64eb0bb34a96682541814b971f053fca17ebfad9db21b98fec4c75db9fe`,
ask-matt `42fd62301dbcfc9154144507d8ced317db95006622d61d5c47d317b0c98f35a4`,
architecture `7a7f052b1935e4d87fc6bbf5f602d3ba136861fec065c4ed0ce55d7f011b1293`,
wayfinder `907f008751ca98a9639d85428606538f865acf33b9a7a3387b5390bfdd1f5e85`다.
진행 중인 제품 코드·원래 목표·평가 정답·승인 규칙은 이번 스킬 작업에서 수정하지 않았다.

## 2026-09-08 지침 정합성 갱신

사용자의 스킬 갱신 요청에 따라 기존 목표 지향 지침과 남아 있던 절차의 충돌을 정리했다.
아래 2026-09-04 점수는 당시 문서의 정적 평가 기록이며, 현재 스킬 점수나 실행 성능을 뜻하지 않는다.

- `odeduck-graph-engineering`: `INTENT.md`, 역할 기반 발견, 원천 연결 검증, 실제 양성 완주의 구분은
  유지한다. 모델 판정과 독립 원천 검증을 구분하고, 추가 정의·대응표의 원문 위치·revision·적용 범위를
  검토 입력까지 연결하도록 보강했다. 적용 범위 지침은 provenance 절에 모으고, 특정 원천의 정답은
  실행 계약·평가 자료에 둔다. 모델의 보류만으로 새 추상화를 추가하지 않는다.
- 공용 `improve-codebase-architecture`: 삭제 판단을 `codebase-design`의 단일 정의에 맞추고
  특정 `Skill` 도구 호출을 현재 환경에서의 참조 읽기로 바꿨다. HTML 생성·브라우저 열기 강제를
  제거하고 필요한 비교에만 시각화를 쓴다. 현 구조 유지·단순화를 기준선에 넣고 범용 추상화는
  설계에 쓴 사례 밖의 평가와 구분해 검토한다.
- 공용 `ask-matt`: 스킬 갱신 요청은 `writing-for-agents`로 연결한다. 기존 spec·tracker 재사용과
  충돌하던 `.scratch/` 경로 지정, 티켓마다 context 초기화, 고정 context 크기 지침을 교체했다.
  스킬 수정은 관찰한 실패와 대체 행동에 묶고, 공용 절차와 프로젝트 계약을 분리한다. 기존 회귀
  시나리오와 다른 맥락의 전이 사례를 사용하며 정적 검사와 실제 행동 검증을 구분한다.
- 공용 `wayfinder`: 도구 이름에 종속된 호출과 무조건 인터뷰를 제거했다. 남은 사용자 결정만 질문하고,
  확정된 기술 작업은 기존 근거에서 이어간다. 독립 조사도 기존 문서 흐름과 작업 공간을 보존한다.
  재개 시 과거 구현 위임보다 최신 요청의 작업 종류를 먼저 확인한다. 저장소의 Wayfinding 라벨과
  issue 관계 규칙은 `docs/agents/issue-tracker.md`와 일치함을 확인해 유지했다.

공용 변경은 이 저장소 밖 `~/.codex/skills/`의 세 스킬과 `ask-matt/PHASE-BOUNDARIES.md`에 적용되며
다른 프로젝트에도 영향을 준다. 변경 전 백업은 `/tmp/odeduck-skill-refresh.2U6WTM/`에 있다.
후속 정리 직전 백업은 `/tmp/odeduck-skill-maintenance.aYOcaI/`에 있다. 임시 백업은 영구 보존을
보장하지 않는다. 이 스킬 정합성 작업에서는 설치·호출 정책, 사업 하네스, 제품 실행 코드, 승인 규칙,
원래 목표와 평가 정답을 변경하지 않았다. 작업 트리의 기존 제품 구현 변경은 별도 미완료 작업이다.

### 검증 범위와 행동 회귀 시나리오

frontmatter·기존 호출 정책·파일 참조·상충 문구 제거의 정적 검사와 `git diff --check`를 통과했다.
아래는 후속 실제 실행의 기대 행동이며 이번 변경의 모델 행동 통과율이 아니다. 새 모델 실행이나
양성 목표 완주 실험은 하지 않았다.

| 입력 상황 | 기대 행동 |
| --- | --- |
| 구현을 위임했고 spec과 실패 기록이 있음 | 재인터뷰·새 지도보다 다음 미충족 기준의 실제 경로 검증으로 진행 |
| 스킬 자체를 고쳐 달라는 요청 | 영향받는 지침을 고치고 제품 목표 미완료는 별도로 유지 |
| 검토만 요청했고 GitHub tracker가 있음 | 읽기 전용 검토; 이슈 생성·할당·닫기와 별도 tracker 생성 없음 |
| 참조 스킬이나 전용 도구가 없음 | 현재 환경의 동등한 절차 사용; 설치를 전제로 작업을 멈추지 않음 |
| 모델은 승인했지만 기간·모집단의 원천 근거가 없음 | 승인과 정확도를 구분하고 누락 근거를 취득·대조 |
| 원천 파일이 새 revision으로 바뀜 | 이전 실패·기대값 보존; 새 원천의 독립 검증을 별도로 수행 |
| 원래 목표를 표본이나 특정 PK 힌트로만 재현함 | 진단으로 기록; 범용 자율 완주로 승격하지 않음 |
| 더 단순한 구조로 교체함 | 호출자·호환 계약 확인 뒤 낡은 활성 경로 정리; 과거 증거 보존 |
| 작은 아키텍처 검토를 요청함 | 현 구조 유지·단순화를 포함해 짧게 비교; HTML·브라우저 실행 강제 없음 |
| 그래프와 무관한 프로젝트에서 범용 추상화를 제안함 | 해당 프로젝트의 서로 다른 사용 사례와 기준선으로 평가; 오데덕 PK·정답 이식 없음 |
| 과거 구현 위임 후 최신 요청이 스킬 갱신으로 바뀜 | 스킬 변경 결과를 반환하고 제품의 미완료 상태는 보존 |

### 후속 정합성 점검

`skill-judge`로 남은 모순을 찾고 `writing-for-agents`에 따라 해당 문장을 교체했다.
공용 architecture의 카드·배지 형식 강제, ask-matt의 작업 디렉터리만으로 인터뷰를 선택하는 분기,
phase-boundaries의 자동 compaction 불허·이전 세션 복구 가능 단정을 제거했다. 문맥 전환은 현재
환경이 제공하는 기능으로 수행하고 미완료 단계·대기 중 결과를 보존한다. 프로젝트 그래프 스킬의
네 층은 의미 경계이며 네 개의 저장소나 클래스를 강제하지 않는다고 명시했다.
이번 후속 편집 전 백업은 `/tmp/odeduck-skill-consistency.DD2y6z/`에 있다.

아래는 현재 문서에 대한 주관적 정적 평가다. 실행 성공률이나 이전 점수 대비 개선율이 아니다.
architecture는 Process, ask-matt는 Navigation, wayfinder와 graph는 Process로 분류했다.
지식 비율(E:A:R)의 대략적인 추정은 각각 65:30:5, 45:40:15, 65:30:5, 80:18:2다.

| 차원 (최대) | architecture | ask-matt | wayfinder | graph | 근거와 남은 한계 |
| --- | ---: | ---: | ---: | ---: | --- |
| 지식 차이 (20) | 16 | 13 | 16 | 18 | 삭제 시험·지도 frontier·원천 시간 계약은 판단을 바꾸지만 router의 일반 절차 설명은 길다 |
| 판단·절차 (15) | 13 | 12 | 13 | 14 | 목표 기준선과 미충족 조건에서 진행하며, router는 경로 설명이 중복된다 |
| 실패 방지 (15) | 13 | 12 | 13 | 14 | 결과 미검증·목표 축소·이름 병합을 구분하나 실제 행동 회귀는 미실행이다 |
| 형식·호출 (15) | 14 | 14 | 14 | 15 | 공용 세 개의 명시 호출 정책과 graph의 조건부 발견 설명을 유지했다 |
| 정보 배치 (15) | 14 | 12 | 13 | 13 | HTML·문맥 전환은 조건부 참조이며 router의 전체 스킬 목록과 graph의 긴 본문은 정리 여지가 있다 |
| 자유도 (15) | 14 | 13 | 13 | 14 | 기준선 유지와 위임·읽기 전용 분기가 있으나 여러 경로를 함께 검증하지 않았다 |
| 패턴 (10) | 9 | 8 | 9 | 9 | 비교·라우팅·결정 지도·query-first 순서가 분명하다 |
| 실제 사용성 (15) | 12 | 11 | 12 | 12 | 참조와 분기를 정적으로 확인했으며 독립 에이전트 행동 시험은 남아 있다 |
| 합계 / 등급 | 105 / B | 95 / C | 103 / B | 109 / A | 등급은 문서 휴리스틱이며 제품 완료나 실행 보증이 아니다 |

우선 후속 검증은 기존 표의 세 분기(최신 요청 전환, 단순 구조 유지, 다른 도메인의 전이 사례)를
실제 에이전트 실행으로 확인하는 것이다. 아직 관찰하지 않은 실패에 대한 지침을 계속 추가하지 않는다.

## 2026-09-04 정적 평가 — 역사적 기록

평가일: 2026-09-04  
평가 기준: `skill-judge` 120점 체계

## odeduck-business-harness

- **점수:** 107/120, B
- **패턴:** Process
- **지식 비율:** Expert 65% / Activation 32% / Redundant 3%
- **판정:** 프로젝트의 데이터 계약과 결제 gate를 잘 결합하지만, 고객조사의 일부는 범용 원칙이라 실제
  인터뷰·결제 artifact가 쌓일수록 오대덕 고유 기준으로 교체해야 한다.

| 차원 | 점수 | 최대 | 근거 |
| --- | ---: | ---: | --- |
| Knowledge delta | 15 | 20 | 정부 무료 시스템, 식별 edge, 결제 등급을 한 gate로 묶은 부분은 고유하나 고객조사 일부는 범용 |
| Mindset + procedure | 13 | 15 | dataset이 아닌 바뀌는 결정에서 시작하고 inspect까지 연결 |
| Anti-patterns | 14 | 15 | 실제 조사에서 반복된 여섯 함정과 이유가 구체적 |
| Specification | 15 | 15 | WHAT/WHEN과 사업·GTM trigger가 frontmatter에 있음 |
| Progressive disclosure | 14 | 15 | 150줄 미만의 self-contained skill, 관련 프로젝트 문서만 조건부 선택 |
| Freedom calibration | 13 | 15 | 발산은 자유롭고 결제·식별 gate는 엄격함 |
| Pattern | 9 | 10 | 단계별 완료 조건이 있는 Process 패턴 |
| Practical usability | 14 | 15 | 결과 형식과 중단 조건이 명확하나 실제 artifact 예시는 아직 없음 |

### 남은 개선

1. 첫 고객 인터뷰와 발주서가 생기면 익명화된 성공·실패 artifact를 기준으로 추가한다.
2. 같은 후보를 두 번 평가한 뒤에도 결과가 흔들리면 evidence 등급별 최소 표본을 조정한다.
3. 고정된 14~30일 실험이 구매 주기가 긴 공공·대기업 고객에 맞지 않는 사례가 나오면 branch를 분리한다.

## odeduck-graph-engineering

- **점수:** 114/120, A
- **패턴:** Process
- **지식 비율:** Expert 84% / Activation 15% / Redundant 1%
- **판정:** source record·canonical entity·claim과 valid/observed time을 분리해 현재 오대덕의 가장 큰
  오연결 위험을 직접 제어한다. 특정 graph database를 미리 선택하지 않은 점도 현재 단계에 맞다.

| 차원 | 점수 | 최대 | 근거 |
| --- | ---: | ---: | --- |
| Knowledge delta | 18 | 20 | 공공데이터의 식별 범위, 정정, coverage, 행동별 오탐을 graph 계약으로 구체화 |
| Mindset + procedure | 15 | 15 | query-first에서 gold set과 저장 선택까지 순서가 비가역성에 맞음 |
| Anti-patterns | 15 | 15 | metadata edge 물질화, 이름 병합, provenance 삭제 등 실제 실패를 직접 차단 |
| Specification | 15 | 15 | entity resolution, provenance, temporal, graph model trigger가 명확함 |
| Progressive disclosure | 14 | 15 | 150줄 미만 self-contained skill이며 외부 runtime reference가 없음 |
| Freedom calibration | 14 | 15 | 모델링 판단은 열어두고 고비용 자동 행동의 식별 규칙은 엄격함 |
| Pattern | 9 | 10 | 완료 기준과 branch가 있는 Process 패턴 |
| Practical usability | 15 | 15 | 평가셋과 결과 계약에 실제 verified ledger schema와 exact/deterministic fixture가 반영됨 |

### 남은 개선

1. 실제 자동 행동별 허용 오탐이 정해지면 현재의 정성 gate를 수치 계약으로 바꾼다.
2. graph 저장 후보를 비교하는 날에는 동일 gold query를 실행하는 benchmark script를 함께 추가한다.

## 종합

두 스킬 모두 프로젝트 로컬에서 독립 실행 가능하다. 사업 하네스는 실제 고객 artifact가 들어오기 전까지
B로 유지하고, graph 하네스는 현재 설계 검토에 사용할 수 있다. 외부 스킬 업데이트를 자동 동기화하지
않으며, 오대덕에서 관찰한 실패가 있을 때만 이 평가와 하네스를 함께 갱신한다.
